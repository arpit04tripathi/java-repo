plugins {
    id 'org.a04t.java_repo.java-application-conventions'
}

version '0.0.1'

dependencies {
    implementation 'org.apache.commons:commons-text'
    implementation project(':common')
}

application {
    // Define the main class for the application.
    mainClass = 'org.a04t.java_repo.algo.App'
}
